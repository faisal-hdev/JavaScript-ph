// function getMenu() {
//   console.log("Burger, Pizza, Pasta");
// }

// getMenu();

/**======= Task-1 ======== */
// Take four parameters. Multiply the four numbers and then return the result
function name(p1, p2, p3, p4) {
  //   const multiply1 = p1 * p2;
  //   const multiply2 = p3 * p4;
  //   const result = multiply1 * multiply2;
  const multiplyResult = p1 * p2 * p3 * p4;
  return multiplyResult;
}
// console.log(name(2, 4, 6, 8));

/**======= Task-2 ======== */
// Take a number if the number is odd multiply it by 2 and return the result. If the number is even divide it by two and return the result.

function number(numbs) {
  console.log(numbs);
  let multiply = 0;
  if (numbs % 2 === 1) {
    //   multiply = multiply * numbs;
    console.log(numbs);
  }
  //   return multiply;
}

const numbs = 22;
const result = number(numbs);
